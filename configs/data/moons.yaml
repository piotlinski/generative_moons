_target_: src.data.moons_datamodule.MoonsDataModule

n_samples: 1000
noise: 0.15
random_state: ${seed}

data_dir: ${paths.data_dir}
batch_size: 128
train_val_test_split: [0.6, 0.2, 0.2]
num_workers: 0
pin_memory: False
